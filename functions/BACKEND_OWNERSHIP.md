# Backend Ownership

Production Firebase infrastructure is owned by the sibling `realX-backend`
repository. This package is retained temporarily for migration history and
must not be deployed to `reelx-backend`.

The root `firebase.json` intentionally contains no Functions, Firestore, or
Storage deployment targets.
